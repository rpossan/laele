<div class="space-y-6">
  <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
    <h2 class="text-2xl font-semibold text-slate-900 mb-2">Escolha a conta Google Ads</h2>
    <p class="text-sm text-slate-500 mb-6">
      Encontramos <%= pluralize(@customer_ids.count, 'conta acessível', 'contas acessíveis') %>.
    </p>

    <%= form_with url: google_ads_auth_select_path, method: :post, local: true, class: "space-y-4" do |f| %>
      <%= hidden_field_tag :google_account_id, @google_account.id %>
      <%= hidden_field_tag :customer_names, @customer_names.to_json %>
      
      <div class="space-y-3">
        <% @customer_ids.each_with_index do |customer_id, index| %>
          <label class="flex items-center gap-4 rounded-xl border border-slate-200 bg-white p-4 shadow-sm hover:border-indigo-200 hover:bg-indigo-50/40 cursor-pointer transition">
            <%= radio_button_tag :login_customer_id, customer_id, index == 0, class: "h-4 w-4 text-indigo-600 focus:ring-indigo-500" %>
            <div class="flex-1">
              <div class="font-semibold text-slate-900">
                <%= format_customer_id(customer_id) %>
              </div>
              <% if @customer_names[customer_id].present? %>
                <div class="mt-1 text-sm text-slate-500">
                  <%= @customer_names[customer_id] %>
                </div>
              <% end %>
            </div>
          </label>
        <% end %>
      </div>

      <div class="flex flex-col gap-3 pt-4 sm:flex-row">
        <%= f.submit "Confirmar seleção", class: "flex-1 inline-flex items-center justify-center rounded-xl bg-indigo-600 px-6 py-3 text-sm font-semibold text-white shadow-sm hover:bg-indigo-700 transition" %>
        <%= link_to "Cancelar", dashboard_path, class: "inline-flex items-center justify-center rounded-xl border border-slate-200 px-6 py-3 text-sm font-medium text-slate-600 hover:text-slate-900 hover:border-slate-300 transition" %>
      </div>
    <% end %>
  </div>

  <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
    <h3 class="text-sm font-semibold text-slate-900 mb-2">Sobre a seleção</h3>
    <ul class="list-disc list-inside space-y-2 text-sm text-slate-600">
      <li>A conta escolhida será aplicada como contexto global até que você troque.</li>
      <li>Você pode conectar novas contas Google Ads a qualquer momento.</li>
      <li>MCCs (Manager Accounts) costumam aparecer primeiro na relação.</li>
    </ul>
  </div>
</div>

