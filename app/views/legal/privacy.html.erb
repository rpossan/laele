<div class="max-w-4xl mx-auto py-12 px-4">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold"><%= t('privacy.title') %></h1>
    <div class="flex gap-2">
      <%= link_to "English", set_locale_path(:en), class: "text-sm px-3 py-1 rounded border #{'bg-indigo-50' if I18n.locale == :en}" %>
      <%= link_to "Português", set_locale_path(:'pt-BR'), class: "text-sm px-3 py-1 rounded border #{'bg-indigo-50' if I18n.locale == :'pt-BR'}" %>
    </div>
  </div>

  <div class="prose prose-slate">
    <%= t('privacy.body_html').html_safe %>
  </div>
</div>
