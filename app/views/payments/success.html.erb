<div class="min-h-[60vh] flex items-center justify-center">
  <div class="max-w-md w-full mx-auto">
    <div class="bg-white rounded-2xl shadow-xl border border-slate-100 p-8 text-center">
      <% if @subscription&.active? %>
        <!-- Subscription Active -->
        <div class="w-20 h-20 mx-auto mb-6 bg-emerald-100 rounded-full flex items-center justify-center">
          <svg class="w-10 h-10 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
          </svg>
        </div>

        <h1 class="text-2xl font-bold text-slate-900 mb-2">Pagamento confirmado!</h1>
        <p class="text-slate-600 mb-6">Sua assinatura do plano <strong><%= @plan&.sub_account_label %></strong> está ativa.</p>

        <div class="bg-emerald-50 border border-emerald-200 rounded-xl p-4 mb-6">
          <div class="flex items-start gap-3">
            <svg class="w-5 h-5 text-emerald-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            <p class="text-sm text-emerald-800">Você já tem acesso completo ao LSA Escale.</p>
          </div>
        </div>

        <% if current_user.google_connected? %>
          <!-- User already has a Google account connected - go to dashboard -->
          <%= link_to leads_path, class: "inline-flex items-center justify-center w-full px-6 py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold rounded-xl transition-colors" do %>
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
            </svg>
            Ir para o Dashboard
          <% end %>
        <% else %>
          <!-- User needs to connect Google account -->
          <div class="bg-blue-50 border border-blue-200 rounded-xl p-4 mb-4">
            <div class="flex items-start gap-3">
              <svg class="w-5 h-5 text-blue-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              <div class="text-left">
                <p class="text-sm font-semibold text-blue-900">Próximo passo</p>
                <p class="text-sm text-blue-800 mt-1">Conecte sua conta Google Ads para começar a gerenciar seus leads.</p>
              </div>
            </div>
          </div>

          <%= link_to google_ads_auth_start_path, class: "inline-flex items-center justify-center w-full px-6 py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold rounded-xl transition-colors", data: { turbo: false } do %>
            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 24 24">
              <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 01-2.2 3.32v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.1z"/>
              <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
              <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
              <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
            </svg>
            Conectar Conta Google Ads
          <% end %>
        <% end %>

      <% else %>
        <!-- Payment Processing -->
        <div class="w-20 h-20 mx-auto mb-6 bg-amber-100 rounded-full flex items-center justify-center">
          <svg class="w-10 h-10 text-amber-600 animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>

        <h1 class="text-2xl font-bold text-slate-900 mb-2">Pagamento sendo processado</h1>
        <p class="text-slate-600 mb-4">Seu pagamento está sendo processado pelo Stripe. Isso geralmente leva alguns segundos.</p>

        <!-- Progress bar animation -->
        <div class="w-full bg-slate-200 rounded-full h-2 mb-6">
          <div class="bg-amber-500 h-2 rounded-full animate-pulse" style="width: 60%"></div>
        </div>

        <div class="bg-amber-50 border border-amber-200 rounded-xl p-4 mb-6">
          <div class="flex items-start gap-3">
            <svg class="w-5 h-5 text-amber-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
            </svg>
            <div class="text-left">
              <p class="text-sm font-semibold text-amber-900">Aguarde, não feche esta página</p>
              <p class="text-sm text-amber-800 mt-1">
                Sua assinatura será ativada automaticamente assim que o pagamento for confirmado.
                Esta página será atualizada automaticamente.
              </p>
            </div>
          </div>
        </div>

        <p class="text-xs text-slate-400 mb-4" id="refresh-status">Verificando status do pagamento...</p>

        <!-- Smart polling instead of full page reload -->
        <script>
          (function() {
            let pollCount = 0;
            const maxPolls = 60; // Stop after 5 minutes (60 * 5s)
            const pollInterval = 5000; // 5 seconds

            function checkStatus() {
              pollCount++;
              const statusEl = document.getElementById('refresh-status');

              if (pollCount >= maxPolls) {
                if (statusEl) {
                  statusEl.textContent = 'O pagamento está demorando mais que o esperado. Recarregue a página manualmente.';
                }
                return;
              }

              if (statusEl) {
                statusEl.textContent = 'Verificando status do pagamento... (' + pollCount + ')';
              }

              fetch('<%= payments_status_path %>', {
                headers: { 'Accept': 'application/json' }
              })
              .then(response => response.json())
              .then(data => {
                if (data.status === 'active') {
                  // Payment confirmed! Reload to show success state
                  window.location.reload();
                } else {
                  // Still pending, poll again
                  setTimeout(checkStatus, pollInterval);
                }
              })
              .catch(() => {
                // On error, try again
                setTimeout(checkStatus, pollInterval);
              });
            }

            // Start polling after 3 seconds
            setTimeout(checkStatus, 3000);
          })();
        </script>

        <!-- Fallback button -->
        <div class="mt-4">
          <%= link_to payments_success_path, class: "inline-flex items-center justify-center w-full px-6 py-3 border border-slate-300 hover:border-slate-400 text-slate-700 font-semibold rounded-xl transition-colors text-sm" do %>
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
            </svg>
            Recarregar página
          <% end %>
        </div>
      <% end %>

      <!-- Stripe Badge -->
      <div class="mt-6 text-center">
        <p class="text-xs text-slate-400 flex items-center justify-center gap-2">
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
          </svg>
          Pagamento seguro processado por Stripe
        </p>
      </div>
    </div>
  </div>
</div>
