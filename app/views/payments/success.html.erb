<div class="min-h-[60vh] flex items-center justify-center">
  <div class="max-w-md w-full mx-auto">
    <div class="bg-white rounded-2xl shadow-xl border border-slate-100 p-8 text-center">
      <% if @subscription&.active? %>
        <!-- Subscription Active -->
        <div class="w-20 h-20 mx-auto mb-6 bg-emerald-100 rounded-full flex items-center justify-center">
          <svg class="w-10 h-10 text-emerald-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
          </svg>
        </div>

        <h1 class="text-2xl font-bold text-slate-900 mb-2">Pagamento confirmado!</h1>
        <p class="text-slate-600 mb-6">Sua assinatura do plano <strong><%= @plan&.sub_account_label %></strong> está ativa.</p>

        <div class="bg-emerald-50 border border-emerald-200 rounded-xl p-4 mb-6">
          <div class="flex items-start gap-3">
            <svg class="w-5 h-5 text-emerald-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            <p class="text-sm text-emerald-800">Você já tem acesso completo ao LSA Escale. Clique abaixo para continuar.</p>
          </div>
        </div>
      <% else %>
        <!-- Payment Processing -->
        <div class="w-20 h-20 mx-auto mb-6 bg-amber-100 rounded-full flex items-center justify-center">
          <svg class="w-10 h-10 text-amber-600 animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
        </div>

        <h1 class="text-2xl font-bold text-slate-900 mb-2">Pagamento sendo processado</h1>
        <p class="text-slate-600 mb-6">Seu pagamento está sendo processado pelo Stripe. Isso geralmente leva alguns segundos.</p>

        <div class="bg-amber-50 border border-amber-200 rounded-xl p-4 mb-6">
          <div class="flex items-start gap-3">
            <svg class="w-5 h-5 text-amber-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
            </svg>
            <p class="text-sm text-amber-800">
              Sua assinatura será ativada automaticamente assim que o pagamento for confirmado.
              Se não ativar em alguns minutos, recarregue esta página ou acesse o dashboard.
            </p>
          </div>
        </div>

        <!-- Auto-refresh for pending -->
        <script>
          // Auto-refresh the page every 5 seconds to check for activation
          let refreshCount = 0;
          const maxRefreshes = 12; // Stop after 1 minute
          const refreshInterval = setInterval(function() {
            refreshCount++;
            if (refreshCount >= maxRefreshes) {
              clearInterval(refreshInterval);
              return;
            }
            window.location.reload();
          }, 5000);
        </script>
      <% end %>

      <%= link_to leads_path, class: "inline-flex items-center justify-center w-full px-6 py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold rounded-xl transition-colors" do %>
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
        </svg>
        Ir para o Dashboard
      <% end %>

      <!-- Stripe Badge -->
      <div class="mt-6 text-center">
        <p class="text-xs text-slate-400 flex items-center justify-center gap-2">
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
          </svg>
          Pagamento seguro processado por Stripe
        </p>
      </div>
    </div>
  </div>
</div>
